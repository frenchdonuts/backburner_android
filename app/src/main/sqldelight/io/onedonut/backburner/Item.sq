CREATE TABLE item (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    text TEXT NOT NULL,
    created_at INTEGER NOT NULL DEFAULT (strftime('%s','now'))
);

insertItem:
INSERT INTO item(text)
VALUES (?);

selectAll:
SELECT *
FROM item
ORDER BY created_at DESC;
